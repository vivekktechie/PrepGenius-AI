# Slim requirements — torch/spacy/transformers omitted (too heavy for free tier)
# Add them back if upgrading to a paid Render instance
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
numpy>=1.26.0
